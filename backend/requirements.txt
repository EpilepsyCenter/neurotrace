fastapi>=0.115.0
uvicorn>=0.32.0
numpy>=1.26.0
scipy>=1.14.0
pyabf>=2.3.8
neo>=0.13.0
myokit>=1.35.0
pydantic>=2.10.0
msgpack>=1.1.0
# Cohort Analysis module (Phase B). Pingouin pulls in pandas + scipy
# stats; pzfx writes Prism XML; openpyxl writes .xlsx. Matplotlib is
# pulled in by pingouin's plotting helpers but we use it directly for
# dot plots + ECDF overlays in Phase B.6.
pingouin>=0.5.5
pzfx>=0.3.0
openpyxl>=3.1.0
pandas>=2.2.0
matplotlib>=3.9.0
pillow>=10.0.0
